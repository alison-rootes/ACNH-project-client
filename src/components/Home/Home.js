import React, { useState } from 'react';
import AuthForm from '../Auth/AuthForm';

const Home = (props) => {
    // const exampleText = "please work, this is a test"

    return (
        <div>
            
        </div>
    )
}

export default Home;